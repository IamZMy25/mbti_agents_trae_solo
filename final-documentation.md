# 多Agents头脑风暴/辩论赛项目文档

## 1. 项目概述

本项目旨在创建一个多Agents进行头脑风暴或辩论赛的系统，每个Agent都具有不同的MBTI人格特征。系统支持三种会话模式：头脑风暴、辩论赛和闲聊，用户可以通过前端界面与这些智能体进行交互。

### 1.1 核心功能
- **多MBTI人格智能体**：每个智能体都具有独特的MBTI人格特征
- **三种会话模式**：头脑风暴、辩论赛和闲聊
- **实时交互**：通过WebSocket实现实时通信
- **数据持久化**：使用PostgreSQL存储会话信息、消息记录、观点和投票
- **可配置性**：用户可以配置LLM API和参与的智能体

### 1.2 技术栈
- **前端**：React + Redux Toolkit + Ant Design + WebSocket
- **后端**：Python 3.11.14 + FastAPI + LangChain 1.2.10 + LangGraph
- **数据库**：PostgreSQL 15-alpine
- **部署**：Docker + Docker Compose

## 2. 需求分析

### 2.1 前端需求
1. **界面设计**：微信/QQ风格的聊天界面
2. **配置功能**：配置LLM的API
3. **会话管理**：
   - 查看已发生过的头脑风暴/辩论赛/闲聊
   - 建立新的头脑风暴/辩论赛/闲聊
4. **交互控制**：
   - 开始/暂停讨论
   - 发送消息

### 2.2 后端需求
1. **Agent设计**：
   - Host Agent：主持人智能体，用于推进讨论，总结观点
   - MBTI Agent：带有MBTI人格的智能体
2. **模式实现**：
   - 头脑风暴Graph：包含开场白、第一轮观点表达、第一轮讨论、观点投票、第二轮讨论、总结等节点
   - 辩论赛Graph：包含开场、正方立论、反方立论、自由辩论、总结陈词、结果宣布等节点
   - 闲聊模式：有记忆的MBTI对话
3. **并发处理**：并发调用LLM，谁先得到输出就谁先回答
4. **数据库存储**：存储用户信息、会话信息、消息记录、观点和投票

## 3. 前端设计

### 3.1 界面结构
- **登录/注册页面**：用户认证
- **主界面**：左侧会话列表，右侧聊天内容区域
- **配置页面**：LLM API配置
- **创建会话页面**：选择会话类型，填写会话信息，选择参与的MBTI人格智能体

### 3.2 交互流程
1. **配置流程**：用户进入配置页面，输入LLM API信息，保存配置
2. **查看历史会话**：用户在左侧会话列表中选择历史会话，右侧显示聊天记录
3. **建立头脑风暴**：用户点击"创建会话"按钮，选择"头脑风暴"类型，输入topic，勾选参与的MBTI人格智能体（最多16个），点击"创建"按钮，系统创建会话，用户点击"开始讨论"按钮开始讨论，用户可以随时点击"暂停讨论"按钮暂停讨论
4. **建立辩论赛**：用户点击"创建会话"按钮，选择"辩论赛"类型，输入辩论议题，勾选正/反方各4位辩手的MBTI人格（可重复），点击"创建"按钮，系统创建会话，用户点击"开始讨论"按钮开始讨论，用户可以随时点击"暂停讨论"按钮暂停讨论
5. **建立闲聊**：用户点击"创建会话"按钮，选择"闲聊"类型，选择闲聊对象的MBTI人格，点击"创建"按钮，系统创建会话，用户可以开始正常聊天

### 3.3 核心组件
- **ChatRoom组件**：显示聊天消息，发送消息，控制讨论
- **CreateSession组件**：创建新会话（头脑风暴/辩论赛/闲聊）
- **SessionList组件**：显示会话列表
- **Settings组件**：配置LLM API

## 4. 后端设计

### 4.1 系统架构
- **API服务**：处理前端请求，提供RESTful API和WebSocket连接
- **会话管理服务**：管理会话生命周期，包括创建、启动、暂停、结束会话
- **LangGraph图执行**：执行三种模式的图流程
- **LLM调用服务**：封装LLM调用，支持并发调用
- **数据库服务**：存储会话信息、消息记录、观点和投票

### 4.2 Agent设计
- **Host Agent**：主持人智能体，用于推进讨论，总结观点
- **MBTI Agent**：带有MBTI人格的智能体，根据MBTI人格生成观点，参与讨论，对观点进行投票

### 4.3 模式实现
#### 4.3.1 头脑风暴Graph
- **开场白节点**：Host Agent根据用户的topic进行开场白
- **第一轮观点表达节点**：多个MBTI Agent对开场白进行观点表达（无记忆）
- **第一轮讨论节点**：多个MBTI Agent进行第一轮讨论（有记忆）
- **观点投票节点**：Host Agent总结观点，MBTI Agent投票
- **第二轮讨论节点**：多个MBTI Agent针对选定观点进行第二轮讨论（有记忆，落地讨论）
- **总结节点**：Host Agent根据前面讨论的内容进行总结

#### 4.3.2 辩论赛Graph
- **开场节点**：Host Agent介绍辩论议题和规则
- **正方立论节点**：正方MBTI Agent依次进行立论
- **反方立论节点**：反方MBTI Agent依次进行立论
- **自由辩论节点**：正反方MBTI Agent进行自由辩论
- **总结陈词节点**：正反方MBTI Agent依次进行总结陈词
- **结果宣布节点**：Host Agent宣布辩论结果

#### 4.3.3 闲聊模式
- **实现**：有记忆的MBTI对话
- **核心功能**：基于MBTI人格的对话生成，对话历史记忆，实时响应

### 4.4 API设计
- **RESTful API**：
  - 用户相关：注册、登录、获取用户信息
  - 会话相关：创建会话、获取会话列表、获取会话详情、开始会话、暂停会话、结束会话
  - 消息相关：获取会话消息、发送消息
- **WebSocket API**：
  - 连接：ws://localhost:8000/ws/sessions/{id}
  - 消息类型：message（新消息）、status（会话状态变更）、error（错误信息）

## 5. 数据库设计

### 5.1 表结构
- **users表**：用户信息
- **sessions表**：会话信息
- **session_members表**：会话成员
- **messages表**：消息记录
- **viewpoints表**：观点记录
- **votes表**：投票记录

### 5.2 索引设计
- **主键索引**：所有表的id字段
- **外键索引**：sessions表的user_id字段，session_members表的session_id字段，messages表的session_id字段，viewpoints表的session_id字段，votes表的session_id和viewpoint_id字段
- **其他索引**：users表的username字段，sessions表的status字段，messages表的timestamp字段，session_members表的agent_type和agent_id字段组合索引

### 5.3 数据库操作
- **插入操作**：记录会话、消息、观点和投票
- **查询操作**：获取历史会话、消息记录、观点和投票结果
- **更新操作**：更新会话状态
- **删除操作**：清理过期会话

## 6. 技术实现

### 6.1 前端实现
- **项目结构**：组件化设计，使用React、Redux Toolkit、Ant Design
- **WebSocket实现**：实时通信，处理消息和状态变更
- **状态管理**：使用Redux Toolkit管理全局状态

### 6.2 后端实现
- **项目结构**：模块化设计，使用FastAPI、LangChain、LangGraph
- **LangGraph实现**：构建三种模式的图流程
- **WebSocket实现**：处理实时通信
- **数据库实现**：使用SQLAlchemy ORM

### 6.3 部署方案
- **Docker Compose配置**：定义服务、网络和卷
- **构建和运行**：使用Docker Compose构建和启动服务
- **访问应用**：前端和后端API的访问地址

## 7. 代码示例

### 7.1 前端代码示例
- **ChatRoom组件**：显示聊天消息，发送消息，控制讨论
- **CreateSession组件**：创建新会话（头脑风暴/辩论赛/闲聊）
- **WebSocketService**：处理实时通信

### 7.2 后端代码示例
- **BrainstormGraph**：头脑风暴图流程实现
- **WebSocket处理**：处理WebSocket连接和消息
- **SQLAlchemy模型**：数据库模型定义
- **FastAPI路由**：API路由定义
- **LLMService**：LLM调用服务
- **MBTIAgent**：MBTI智能体实现

## 8. 总结

本项目实现了一个多Agents头脑风暴/辩论赛系统，具有以下特点：

1. **多MBTI人格智能体**：每个智能体都具有独特的MBTI人格特征，能够根据不同的人格生成不同的观点和讨论内容。

2. **三种会话模式**：支持头脑风暴、辩论赛和闲聊三种会话模式，满足不同的使用场景。

3. **实时交互**：通过WebSocket实现实时通信，确保讨论的流畅性和实时性。

4. **数据持久化**：使用PostgreSQL存储会话信息、消息记录、观点和投票，确保数据的持久化和可靠性。

5. **可配置性**：用户可以配置LLM API和参与的智能体，满足不同的需求。

6. **易于部署**：使用Docker Compose简化环境配置和部署流程，使系统能够快速部署到生产环境。

该系统可以为用户提供一个有趣、实用的多Agents交互平台，帮助用户进行头脑风暴、辩论赛和闲聊等活动，激发创意和思考。